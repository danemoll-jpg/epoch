# Sounds go here

Drop Dan's MP3 files into this folder with the exact names in `docs/SOUNDS.md`
(e.g. `tech-learned.mp3`, `music-1.mp3`). The next build bundles whatever is here; a
missing file plays nothing. Check them on `docs/sounds.html`.
